SELECT * FROM "{{JSutils.currentSchema()}}".hotufs001
WHERE ufs01_deleted <> true
ORDER BY
{{
  ({
    ufs01_ufnome: 'ufs01_ufnome',
    ufs01_uf: 'ufs01_uf'
  }[TableVeterinario.sortOrder.column]) || 'ufs01_ufnome'
}}
{{ TableVeterinario.sortOrder.order === 'DESC' ? 'DESC' : 'ASC' }};
