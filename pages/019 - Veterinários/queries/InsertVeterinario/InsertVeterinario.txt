INSERT INTO "{{JSutils.currentSchema()}}".hotvet001 (
	vet001_id,
	vet001_nome,
	vet001_crmv,
	vet001_endereco,
	vet001_bairro,
	vet001_numero,
	vet001_complemento,
	vet001_cidade,
	vet001_email,
	vet001_telefone,
	vet001_celular,
	vet001_uf,
	vet001_cep,
	vet001_comissao,
	vet001_clinica,
	vet001_creation_date,
	vet001_created_by,
	vet001_last_modification,
	vet001_modified_by)
VALUES (
	nextval('hotvet001_vet001_id_seq'::regclass),
	'{{JSconfirmation.modalconfig.row.vet001_nome?.trim().toUpperCase()}}',
	{{JSconfirmation.modalconfig.row.vet001_crmv || 0}},
	'{{JSconfirmation.modalconfig.row.vet001_endereco?.trim().toUpperCase()}}',
	'{{JSconfirmation.modalconfig.row.vet001_bairro?.trim().toUpperCase()}}',
	'{{JSconfirmation.modalconfig.row.vet001_numero?.trim().toUpperCase()}}',
	'{{JSconfirmation.modalconfig.row.vet001_complemento?.trim().toUpperCase()}}',
	'{{JSconfirmation.modalconfig.row.vet001_cidade?.trim().toUpperCase()}}',
	'{{JSconfirmation.modalconfig.row.vet001_email?.trim().toLowerCase()}}',
	'{{JSconfirmation.modalconfig.row.vet001_telefone?.trim().toUpperCase().replace(/\D/g, "")}}',
	'{{JSconfirmation.modalconfig.row.vet001_celular?.trim().toUpperCase().replace(/\D/g, "")}}',
	'{{JSconfirmation.modalconfig.row.vet001_uf?.trim().toUpperCase()}}',
	'{{JSconfirmation.modalconfig.row.vet001_cep?.trim().toUpperCase().replace(/\D/g, "")}}',
	'0',
	{{JSconfirmation.modalconfig.row.vet001_clinica || 'NULL'}},
	now(),
	{{ appsmith.user.id || 1 }},
	now(),
	{{ appsmith.user.id || 1 }});
